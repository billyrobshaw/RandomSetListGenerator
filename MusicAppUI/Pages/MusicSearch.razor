@page "/"
@using MusicApp.UI2.Models
@using MusicApp.UI2.Services
@inject MusicService MusicService
@inject IJSRuntime JS
@implements IDisposable

@if (showIntro)
{
    <div class="text-center mt-5" style="background-color: #fff7e6;">
        <h3 class="fw-bold">🎵 Preparing your first song...</h3>
        <p class="mt-3">Starting in @introCountdown seconds</p>

        <p class="mt-4">
            👉 <strong>First Song:</strong> @music[currentIndex].SongTitle<br />
            <span class="text-muted">@music[currentIndex].Artist</span>
        </p>
    </div>
}
else if (music != null && music.Count > 0)
{
    <div class="page-wrapper" style="background-color: #fff7e6;">
        <div class="content-wrapper">

            <div class="card shadow-sm text-center p-4">

                <h2 class="fw-bold">@music[currentIndex].SongTitle</h2>
                <h5 class="text-muted">@music[currentIndex].Artist</h5>

                <div id="lyricsBox" class="lyrics-box mt-4 mx-auto">
                    <pre class="lyrics-text">@music[currentIndex].Lyrics</pre>
                </div>

                @if (!string.IsNullOrWhiteSpace(music[currentIndex].Chords))
                {
                    <div class="chords-box mt-4 mx-auto">
                        <strong>Chords</strong>
                        <div class="mt-2">@music[currentIndex].Chords</div>
                    </div>
                }

                @if (showTransitionMessage)
                {
                    <p class="fw-bold text-primary mt-3">
                        🎵 Get ready for the next song...
                    </p>
                    <p class="text-muted">Next song starts in @transitionRemaining seconds</p>

                    @if (nextIndex != null)
                    {
                        <p class="mt-3">
                            👉 <strong>Coming Up:</strong> @music[nextIndex.Value].SongTitle
                            <br />
                            <span class="text-muted">@music[nextIndex.Value].Artist</span>
                        </p>
                    }
                }


            </div>
        </div>

        <!-- FOOTER pinned to bottom -->
        <div class="footer-info text-center mt-3">
            <p class="small text-muted">
                ⏱ Duration: @music[currentIndex].Duration.ToString(@"mm\:ss")
            </p>
            <p class="fw-bold text-danger">
                ⏳ Time Remaining: @remainingTime.ToString(@"mm\:ss")
                <button class="btn btn-primary mt-3" @onclick="SkipSong">
                    ⏭ Skip
                </button>
            </p>
            
        </div>
    </div>

}

@code {
    private List<MusicDto>? music;
    private bool loading = true;
    private int currentIndex = 0;
    private TimeSpan remainingTime;
    private int? nextIndex = null;
    private bool showIntro = true;
    private int introCountdown = 10;


    private CancellationTokenSource? cts;

    // NEW: transition state
    private bool showTransitionMessage = false;
    private int transitionRemaining = 0;

    protected override async Task OnInitializedAsync()
    {
        Console.WriteLine("STEP 1: OnInitializedAsync started");

        music = await MusicService.GetAllMusic();
        loading = false;

        if (music != null && music.Count > 0)
        {
            var rnd = new Random();
            currentIndex = rnd.Next(0, music.Count);

            // Show intro message for 10 seconds
            introCountdown = 10;
            showIntro = true;

            while (introCountdown > 0)
            {
                await Task.Delay(1000);
                introCountdown--;
                await InvokeAsync(StateHasChanged);
            }

            showIntro = false;

            // Start the normal loop
            cts = new CancellationTokenSource();
            _ = PlaySongsLoop(cts.Token);
        }
    }



    private async Task PlaySongsLoop(CancellationToken token)
    {
        Console.WriteLine("STEP 3: Entered PlaySongsLoop");

        try
        {
            while (!token.IsCancellationRequested)
            {
                var song = music![currentIndex];
                remainingTime = song.Duration;

                Console.WriteLine($"STEP 3: Song '{song.SongTitle}' duration = {remainingTime}");

                if (remainingTime.TotalSeconds <= 0)
                    remainingTime = TimeSpan.FromSeconds(1);

                // SONG COUNTDOWN WITH SMOOTH AUTO-SCROLL
                while (remainingTime.TotalSeconds > 0 && !token.IsCancellationRequested)
                {
                    var total = song.Duration.TotalSeconds;
                    if (total <= 0)
                    {
                        total = 1; // avoid divide by zero
                    }

                    var elapsed = total - remainingTime.TotalSeconds;
                    if (elapsed < 0) elapsed = 0;
                    if (elapsed > total) elapsed = total;

                    var progress = elapsed / total; // 0 → 1

                    await JS.InvokeVoidAsync("scrollLyricsSmooth", "lyricsBox", 0.5);


                    await Task.Delay(125, token); // 4× faster updates 
                    remainingTime -= TimeSpan.FromMilliseconds(125);

                    await InvokeAsync(StateHasChanged);
                }

                Console.WriteLine("STEP 3: Song finished");

                // 15 SECOND TRANSITION
                showTransitionMessage = true;
                transitionRemaining = 15;

                // NEW: pick next song early
                var rnd = new Random();
                nextIndex = rnd.Next(0, music.Count);

                await InvokeAsync(StateHasChanged);


                while (transitionRemaining > 0 && !token.IsCancellationRequested)
                {
                    Console.WriteLine($"Transition countdown: {transitionRemaining}");
                    await Task.Delay(1000, token);
                    transitionRemaining--;
                    await InvokeAsync(StateHasChanged);
                }

                showTransitionMessage = false;

                currentIndex = nextIndex ?? new Random().Next(0, music.Count);
                nextIndex = null;

                // Reset scroll for the new song
                await JS.InvokeVoidAsync("resetLyricsScroll", "lyricsBox", 0.5);

                Console.WriteLine("STEP 3: Moving to next song");
            }
        }
        catch (TaskCanceledException)
        {
            Console.WriteLine("STEP 3: Timer cancelled");
        }
    }

    public void Dispose()
    {
        Console.WriteLine("STEP 4: Component disposing, cancelling token");
        cts?.Cancel();
        cts?.Dispose();
    }

    private async Task SkipSong()
    {
        cts?.Cancel();
        cts?.Dispose();

        var rnd = new Random();
        currentIndex = rnd.Next(0, music!.Count);

        // Reset scroll
        await JS.InvokeVoidAsync("resetLyricsScroll", "lyricsBox", 0.5);

        // Reset timer for new song
        remainingTime = music[currentIndex].Duration;

        showTransitionMessage = false;

        cts = new CancellationTokenSource();
        _ = PlaySongsLoop(cts.Token);

        await InvokeAsync(StateHasChanged);
    }



}